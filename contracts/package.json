{
  "name": "purepix-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for PurePix NFT marketplace",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:mumbai": "hardhat run scripts/deploy.js --network mumbai",
    "deploy:base": "hardhat run scripts/deploy.js --network baseGoerli",
    "verify:mumbai": "hardhat verify --network mumbai",
    "verify:base": "hardhat verify --network baseGoerli"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0"
  }
}